# Latest Updates — Octubre 2025

- Documentación sincronizada con contrato `/health`, `/fit_ok`, `/calibrate_ng`, `/infer`.
- PatchCore ejecutándose con DINOv2 ViT-S/14 (`model_version=2025.4`).
- GUI consolidó flujo Freeze/Editar ROI y muestra `request_id` en logs.
- Datasets y manifests describen `mm_per_px`, `shape`, `threshold` por ROI.
- Dockerfiles actualizados (CPU/GPU) y guía de despliegue revisada.
- Roadmap: migrar GUI a .NET 8, batch inferencia, métricas avanzadas.
